import { error } from "console";

const routes = {
  home: '/',
  transacoes: '/transactions',
  cadastro: '/panel',
  cards: '/cards',
  documentos: '/documents',
  error404: '/error',
};

export default routes;